{
  "name": "@soroban-amm/health-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Off-chain pool health monitoring dashboard for the Soroban AMM",
  "scripts": {
    "dev": "npx serve . -p 3000",
    "build": "node --check src/dashboard.js && node --check src/dom-stub.js && test -f styles.css",
    "lint": "node --check src/dashboard.js && node --check src/dom-stub.js",
    "test": "node --test src/dashboard.test.js"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
